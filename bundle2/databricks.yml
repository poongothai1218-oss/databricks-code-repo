# This is a Databricks asset bundle definition for bundle2.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: bundle2
  uuid: 2f940a3e-4501-42cb-8bfe-20280cec7430

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-4d3cbc18-532b.cloud.databricks.com
    variables:
      catalog: workspace
      schema: bb
  prod:
    mode: production
    workspace:
      host: https://dbc-4d3cbc18-532b.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/poongothai1218@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/poongothai1218@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: progcatalog1
      schema: logistics1
    permissions:
      - user_name: poongothai1218@gmail.com
        level: CAN_MANAGE
